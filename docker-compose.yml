services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    environment:
      - PORT=8080
    expose:
      - "8080"

  frontend:
    build:
      context: ./frontend
    env_file:
      - ./frontend/.env
    expose:
      - "3000"
