openapi: 3.0.0
info:
  title: Hearoom API
  version: 1.0.0
  license:
    name: Private
servers:
  - url: https://localhost:8080 # TODO Update
tags:
  - name: system
    description: System related endpoints
  - name: upload
    description: 파일 업로드 관련 엔드포인트
paths:
  /system/health:
    get:
      $ref: './system/health.yaml'
  /upload/presigned-url:
    post:
      $ref: './upload/presigned-url.yaml'

components:
  schemas:
    ErrorResponse:
      $ref: './_schema/common/error-response.yaml#/ErrorResponse'