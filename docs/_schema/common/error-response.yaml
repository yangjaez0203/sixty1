ErrorResponse:
  type: object
  required:
    - code
    - message
  properties:
    code:
      type: string
      description: 에러 코드 (UPPER_SNAKE_CASE)
      example: "INVALID_REQUEST"
    message:
      type: string
      description: 에러 상세 메시지
      example: "userId must be a string"
    data:
      type: object
      description: 부가적인 에러 정보 (optional)
      additionalProperties: true
      nullable: true
